language: node_js
node_js:
  - "0.8"
  - "0.10"
script:
  - "npm run test-travis"
after_script:
- "npm install coveralls@2.11.x && cat coverage/lcov.info | coveralls"
before_install:
  - npm install -g npm
